import styled from "styled-components";
import { animated } from "react-spring";

export const ActionHandler = styled(animated.button)`
  border: 0;
  background: transparent;
  border-radius: 0px;
  position: absolute;
  top: 16px;
  right: 16px;
  outline: none;
  width: 24px;
  height: 24px;
`;
